<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Storj_Tools.NodesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Storj_Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="20">

        <!-- Nadpis -->
        <TextBlock Text="Node List" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Seznam nodů 
        <ListView x:Name="nodeListView" ItemsSource="{x:Bind Nodes}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:NodeData">
                    <StackPanel Orientation="Horizontal" Spacing="10" Padding="5">
-->
                        <!-- Ikonka stavu 
                        <FontIcon Glyph="{x:Bind StatusIcon}" FontSize="20" Foreground="{x:Bind StatusColor}" VerticalAlignment="Center"/>
-->
                        <!-- Název služby
                        <TextBlock Text="{x:Bind ServiceName}" FontSize="16" FontWeight="Bold" Width="150"/>
 -->
                        <!-- Verze služby 
                        <TextBlock Text="{x:Bind ServiceVersion}" FontSize="14" Width="80" VerticalAlignment="Center"/>
-->
                        <!-- Popis stavu 
                        <TextBlock Text="{x:Bind StatusDescription}" FontSize="14" Width="200" VerticalAlignment="Center"/>
-->
                        <!-- Tlačítko pro spuštění 
                        <Button Content="Start" Click="StartService_Click" CommandParameter="{x:Bind ServiceName}"/>

                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->

    </StackPanel>
</Page>
